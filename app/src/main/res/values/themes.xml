<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Mapatec" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/verde</item>
    </style>

    <style name="Theme.Mapatec" parent="Base.Theme.Mapatec" />

    <style name="BottonNavigation">
        <item name="android:background">@color/verde</item>
        <item name="itemIconTint" >@color/white</item>
        <item name="itemTextColor">@color/white</item>
    </style>

    <style name="radius">
        <item name="cornerSizeBottomRight">150dp</item>
        <!-- Para las imagenes con borde -->
    </style>

</resources>